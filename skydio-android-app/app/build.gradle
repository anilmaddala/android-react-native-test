plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)
    alias(libs.plugins.ksp)
    alias(libs.plugins.hilt)
    alias(libs.plugins.protobuf)
    alias(libs.plugins.react.native)
}

// React Native version
def reactNativeVersion = "0.81.5"
def jsProjectRoot = file("$rootDir/../skydio-rn-business-logic")

android {
    namespace 'com.example.skydioandroidapp'
    compileSdk 36

    defaultConfig {
        applicationId "com.example.skydioandroidapp"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

        // React Native build config - enable new architecture
        buildConfigField "boolean", "IS_NEW_ARCHITECTURE_ENABLED", "true"
        buildConfigField "boolean", "IS_HERMES_ENABLED", "true"
        buildConfigField "boolean", "IS_EDGE_TO_EDGE_ENABLED", "false"
    }

    buildTypes {
        debug {
            // Enable developer support in debug builds
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
    buildFeatures {
        compose true
        buildConfig true
    }

    sourceSets {
        main {
            proto {
                srcDir '../../proto'
            }
            // Add assets directory for JS bundle
            assets.srcDirs = ['src/main/assets']
        }
    }
}

// Protobuf configuration
protobuf {
    protoc {
        artifact = "com.google.protobuf:protoc:${libs.versions.protobuf.get()}"
    }
    generateProtoTasks {
        all().configureEach { task ->
            task.builtins {
                create("java") {
                    option("lite")
                }
                create("kotlin") {
                    option("lite")
                }
            }
        }
    }
}

// React Native configuration
react {
    root = jsProjectRoot
    reactNativeDir = file("$jsProjectRoot/node_modules/react-native")
    codegenDir = file("$jsProjectRoot/node_modules/@react-native/codegen")
    cliFile = file("$jsProjectRoot/node_modules/react-native/cli.js")
    entryFile = file("$jsProjectRoot/index.js")
    hermesCommand = "$jsProjectRoot/node_modules/react-native/sdks/hermesc/%OS-BIN%/hermesc"
}

// Auto-install JS dependencies if needed
tasks.register("installJsDependencies", Exec) {
    workingDir jsProjectRoot
    commandLine "npm", "install"

    // Only run if node_modules doesn't exist or package.json is newer
    def nodeModules = file("$jsProjectRoot/node_modules")
    def packageJson = file("$jsProjectRoot/package.json")
    inputs.file(packageJson)
    outputs.dir(nodeModules)

    onlyIf { !nodeModules.exists() || packageJson.lastModified() > nodeModules.lastModified() }
}

// Make React Native tasks depend on JS dependencies
tasks.configureEach { task ->
    if (task.name.contains("bundle") || task.name.contains("Bundle") ||
        task.name.contains("Hermes") || task.name.contains("ReactNative")) {
        task.dependsOn("installJsDependencies")
    }
}

dependencies {
    // React Native
    implementation("com.facebook.react:react-android:$reactNativeVersion")
    implementation("com.facebook.react:hermes-android:$reactNativeVersion")

    // Coroutines for async operations
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:1.7.3"
    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3"

    // Protobuf (lite runtime)
    implementation libs.protobuf.javalite
    implementation libs.protobuf.kotlin.lite

    // Hilt
    implementation libs.hilt.android
    ksp libs.hilt.compiler

    // AndroidX
    implementation libs.androidx.core.ktx
    implementation libs.androidx.lifecycle.runtime.ktx
    implementation libs.androidx.activity.compose
    implementation "androidx.fragment:fragment-ktx:1.8.5"

    // Compose
    implementation platform(libs.androidx.compose.bom)
    implementation libs.androidx.compose.ui
    implementation libs.androidx.compose.ui.graphics
    implementation libs.androidx.compose.ui.tooling.preview
    implementation libs.androidx.compose.material3
    implementation libs.androidx.compose.material3.adaptive.navigation.suite

    // Testing
    testImplementation libs.junit
    androidTestImplementation libs.androidx.junit
    androidTestImplementation libs.androidx.espresso.core
    androidTestImplementation platform(libs.androidx.compose.bom)
    androidTestImplementation libs.androidx.compose.ui.test.junit4
    debugImplementation libs.androidx.compose.ui.tooling
    debugImplementation libs.androidx.compose.ui.test.manifest
}
